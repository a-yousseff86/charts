apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ quote .Values.name }}
  name: {{ quote .Values.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ quote .Values.name }}
  template:
    metadata:
      labels:
        app: {{ quote .Values.name }}
    spec:
      containers:
        - image: {{ .Values.docker_image }}
          imagePullPolicy: Always
          name: client
          ports:
            - containerPort: 80
              protocol: TCP

